# Demo Data

User-facing demo and test data lives here.

The Import CSV/TXT workflow prefers `demo_data/iv_traces/` as its starting folder when the local runtime supports opening an OS file picker. Users can still browse anywhere and import their own CSV/TXT/DAT files.

Publication-derived CSV files should include citation and license notes, ideally as sidecar `.meta.json` files.
