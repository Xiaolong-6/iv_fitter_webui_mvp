# Demo IV Traces

This is the default folder for Import CSV/TXT when the local runtime can control the OS file picker.

Files here should be safe, user-facing demo IV traces. Users still manually choose which file to import, and selected files continue through the normal import/parser pipeline.

`minimal_demo_iv.csv` is a tiny synthetic-looking IV table for smoke testing the import workflow. It is not publication data.
